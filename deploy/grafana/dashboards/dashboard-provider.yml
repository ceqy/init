# Grafana Dashboard 配置

apiVersion: 1

providers:
  - name: 'Envoy Dashboards'
    orgId: 1
    folder: 'Envoy'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
