feat: 完成密码重置流程实现

## 新增功能

### 领域层
- PasswordResetService - 密码重置领域服务
  * generate_reset_token() - 生成重置令牌（32字节随机，SHA256哈希）
  * verify_reset_token() - 验证令牌并标记为已使用
  * revoke_all_tokens() - 撤销用户所有令牌
  * cleanup_expired_tokens() - 清理过期令牌
  * 防滥用：每用户最多3个未使用令牌
  * 完整的单元测试

### 基础设施层
- PostgresPasswordResetRepository - PostgreSQL 仓储实现
  * 实现所有仓储接口方法
  * 租户隔离（通过 JOIN users 表）
  * 完整的错误处理和日志
  * 集成测试（2个测试用例）

### 应用层
- RequestPasswordResetCommand - 请求密码重置命令
- ResetPasswordCommand - 重置密码命令
- RequestPasswordResetHandler - 请求密码重置处理器
  * 生成令牌并发送邮件
  * 防用户枚举（用户不存在也返回成功）
  * 单元测试（2个测试用例）
- ResetPasswordHandler - 重置密码处理器
  * 验证令牌并更新密码
  * 自动撤销所有重置令牌
  * 单元测试（2个测试用例）

### 邮件模板
- password_reset.html - HTML 邮件模板
  * 响应式设计
  * 清晰的重置按钮
  * 安全提示和过期时间
- password_reset.txt - 纯文本邮件模板
  * 备用文本版本

## 安全特性

### 令牌安全
- 32字节（256位）随机令牌
- 存储 SHA256 哈希而非原始令牌
- 令牌一次性使用
- 可配置过期时间（默认15分钟）

### 防滥用
- 每用户最多3个未使用令牌
- 超过限制返回 RESOURCE_EXHAUSTED 错误
- 自动清理过期令牌

### 防用户枚举
- 请求重置时不暴露用户是否存在
- 统一的成功响应

### 租户隔离
- 所有操作强制租户隔离
- 通过 JOIN users 表验证租户
- 防止跨租户访问

## 工作流程

### 请求密码重置
1. 用户输入邮箱
2. 系统查找用户（带租户隔离）
3. 验证用户状态（Active）
4. 检查未使用令牌数量（≤3）
5. 生成32字节随机令牌
6. 计算 SHA256 哈希并存储
7. 发送重置邮件（包含令牌链接）
8. 返回成功（不暴露用户是否存在）

### 重置密码
1. 用户点击邮件中的重置链接
2. 系统验证令牌哈希
3. 检查令牌有效性（未使用、未过期）
4. 标记令牌为已使用
5. 查找用户并验证邮箱匹配
6. 验证新密码强度
7. 更新用户密码
8. 撤销该用户的所有重置令牌
9. 返回成功

## 测试覆盖

### 单元测试（12个测试用例）
- PasswordResetToken 实体测试（4个）
- PasswordResetService 服务测试（2个）
- RequestPasswordResetHandler 处理器测试（2个）
- ResetPasswordHandler 处理器测试（2个）
- PostgresPasswordResetRepository 仓储测试（2个）

### 测试场景
- ✅ 生成和验证令牌
- ✅ 令牌过期处理
- ✅ 令牌已使用处理
- ✅ 防止滥用（超过3个令牌）
- ✅ 用户不存在处理
- ✅ 无效令牌处理
- ✅ 邮箱不匹配处理
- ✅ 租户隔离验证

## 文件变更

### 新增文件（8个）
- services/iam-identity/src/auth/domain/services/password_reset_service.rs
- services/iam-identity/src/auth/infrastructure/persistence/postgres_password_reset_repository.rs
- services/iam-identity/src/auth/application/commands/request_password_reset_command.rs
- services/iam-identity/src/auth/application/commands/reset_password_command.rs
- services/iam-identity/src/auth/application/handlers/request_password_reset_handler.rs
- services/iam-identity/src/auth/application/handlers/reset_password_handler.rs
- crates/adapters/email/templates/password_reset.html
- crates/adapters/email/templates/password_reset.txt

### 修改文件（3个）
- services/iam-identity/src/auth/domain/services/mod.rs
- services/iam-identity/src/auth/application/commands/mod.rs
- services/iam-identity/src/auth/application/handlers/mod.rs

### 已存在文件（使用现有）
- services/iam-identity/src/auth/domain/entities/password_reset_token.rs
- services/iam-identity/src/auth/domain/repositories/password_reset_repository.rs
- services/iam-identity/migrations/20260126021500_create_password_reset_tokens_table.sql
- crates/adapters/email/* (邮件客户端和模板引擎)

## 后续工作

- [ ] 实现 gRPC API 方法（RequestPasswordReset、ResetPassword）
- [ ] 添加 Prometheus 监控指标
- [ ] 添加速率限制（防止邮件轰炸）
- [ ] 支持多语言邮件模板

## 相关文档

- PASSWORD_RESET_COMPLETION.md - 完整实施报告
- docs/api/iam/auth-service.md - API 文档
- docs/guides/security.md - 安全最佳实践
