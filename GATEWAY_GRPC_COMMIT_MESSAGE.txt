feat: 实现 Gateway gRPC 集成和 REST API

完成 Gateway 与 IAM Identity 服务的 gRPC 集成，提供完整的 REST API 接口。

## 主要变更

### 1. gRPC 反射实现（IAM Identity）
- 添加 tonic-reflection 0.13 依赖
- 更新 build.rs 生成文件描述符集（.bin 文件）
- 在 mod.rs 中导出 FILE_DESCRIPTOR_SET
- 在 main.rs 中注册反射服务
- 修复 3 个 tenant_id 字段访问错误
- 添加 extract_tenant_id() 辅助方法

### 2. Gateway gRPC 客户端集成
- 添加 prost、prost-types、uuid 依赖
- 创建 build.rs 编译 proto 文件
- 实现 GrpcClients 管理器连接 IAM 服务
- 配置 gRPC Channel 和客户端

### 3. REST API 实现
- POST /api/auth/register - 用户注册
- POST /api/auth/login - 用户登录
- POST /api/auth/logout - 用户登出
- POST /api/auth/refresh - 刷新 Token
- GET /health - 健康检查
- GET /ready - 就绪检查

### 4. 请求/响应转换
- REST JSON ↔ gRPC Protobuf 自动转换
- gRPC 错误码映射为 HTTP 状态码：
  - Unauthenticated → 401
  - PermissionDenied → 403
  - InvalidArgument → 400
  - AlreadyExists → 409
  - Internal → 500
- 租户 ID 自动注入到 gRPC metadata

### 5. 配置管理
- 添加 IAM_ENDPOINT 环境变量
- 默认连接 http://127.0.0.1:50051
- 支持自定义 Gateway 端口和地址

## 技术细节

### IAM Identity 修复
```rust
// 添加 extract_tenant_id 辅助方法
fn extract_tenant_id<T>(request: &Request<T>) -> Result<TenantId, Status> {
    let tenant_id_str = request
        .metadata()
        .get("tenant-id")
        .and_then(|t| t.to_str().ok())
        .ok_or_else(|| Status::invalid_argument("Missing tenant-id in metadata"))?;

    let uuid = Uuid::parse_str(tenant_id_str)
        .map_err(|_| Status::invalid_argument("Invalid tenant ID format"))?;

    Ok(TenantId::from_uuid(uuid))
}
```

### Gateway 实现
```rust
// gRPC 客户端初始化
let grpc_clients = grpc::GrpcClients::new(config.iam_endpoint.clone())
    .await
    .expect("Failed to connect to IAM service");

// REST 到 gRPC 转换
let mut grpc_req = Request::new(grpc::auth::LoginRequest {
    username: req.username.clone(),
    password: req.password,
    tenant_id: req.tenant_id.clone(),
    device_info: "gateway".to_string(),
    ip_address: "127.0.0.1".to_string(),
});

grpc_req.metadata_mut().insert(
    "tenant-id",
    MetadataValue::try_from(&req.tenant_id)?,
);
```

## 测试验证

### gRPC 反射测试
```bash
# 列出服务
grpcurl -plaintext localhost:50051 list
# 输出：cuba.iam.auth.AuthService, cuba.iam.user.UserService, grpc.reflection.v1.ServerReflection

# 查看方法
grpcurl -plaintext localhost:50051 describe cuba.iam.auth.AuthService.Login
```

### REST API 测试
```bash
# 注册用户
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test_user","email":"test@example.com","password":"TestPass123!","display_name":"Test User"}'

# 登录
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"test_user","password":"TestPass123!"}'

# 登出
curl -X POST http://localhost:8080/api/auth/logout \
  -H "Content-Type: application/json" \
  -d '{"access_token":"<token>","logout_all_devices":false}'
```

## 文件变更

### IAM Identity
- services/iam-identity/Cargo.toml
- services/iam-identity/build.rs
- services/iam-identity/src/main.rs
- services/iam-identity/src/auth/api/grpc/mod.rs
- services/iam-identity/src/auth/api/grpc/auth_service_impl.rs
- services/iam-identity/src/user/api/grpc/mod.rs

### Gateway
- gateway/Cargo.toml
- gateway/build.rs
- gateway/src/main.rs
- gateway/src/config.rs
- gateway/src/auth.rs
- gateway/src/grpc/mod.rs (新增)

### 文档
- GRPC_REFLECTION_STATUS.md (新增)
- GRPC_API_TESTING_GUIDE.md (新增)
- REST_API_TESTING_GUIDE.md (新增)
- GATEWAY_IMPLEMENTATION_SUMMARY.md (新增)

## 影响范围

- ✅ gRPC 反射功能完全可用
- ✅ Gateway REST API 基础功能完成
- ✅ 用户注册、登录、登出流程正常
- ✅ Token 刷新功能正常
- ✅ 错误处理和映射完善
- ✅ CORS 支持
- ✅ 日志追踪

## 后续工作

- [ ] 实现 Token 验证中间件
- [ ] 实现 GET /api/auth/me
- [ ] 添加更多用户管理 API
- [ ] 实现密码管理 API
- [ ] 实现会话管理 API
- [ ] 实现 2FA API
- [ ] 添加速率限制
- [ ] 添加 Metrics 监控

## 测试说明

1. 启动 IAM Identity 服务：`just iam`
2. 启动 Gateway：`cd gateway && cargo run`
3. 使用 curl 或 Postman 测试 REST API
4. 使用 grpcurl 测试 gRPC 反射

## 相关文档

- [GRPC_REFLECTION_STATUS.md](GRPC_REFLECTION_STATUS.md) - gRPC 反射实现状态
- [GRPC_API_TESTING_GUIDE.md](GRPC_API_TESTING_GUIDE.md) - gRPC API 测试指南
- [REST_API_TESTING_GUIDE.md](REST_API_TESTING_GUIDE.md) - REST API 测试指南
- [GATEWAY_IMPLEMENTATION_SUMMARY.md](GATEWAY_IMPLEMENTATION_SUMMARY.md) - Gateway 实现总结
