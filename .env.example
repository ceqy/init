# Application
APP_ENV=development
APP_NAME=cuba-erp

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/cuba
# Connection pool size (default: 10 for dev, 50 for production)
# Recommended: 50-100 for production, adjust based on load
DATABASE_MAX_CONNECTIONS=10

# Redis
# SECURITY: Use password authentication in production
# Format: redis://:password@host:port or redis://host:port (no auth for local dev)
REDIS_URL=redis://localhost:6379

# Kafka
KAFKA_BROKERS=localhost:9092

# ClickHouse
CLICKHOUSE_URL=http://localhost:8123
CLICKHOUSE_DATABASE=cuba

# Auth
# SECURITY: Generate a secure random key (at least 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_TO_A_SECURE_RANDOM_KEY_AT_LEAST_32_CHARS_LONG
JWT_EXPIRES_IN=3600
REFRESH_TOKEN_EXPIRES_IN=604800

# Telemetry
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
LOG_LEVEL=info

# Gateway
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8080

# CORS Configuration
# SECURITY: In production, specify exact allowed origins (comma-separated)
# Leave empty for permissive mode (development only)
# Example: http://localhost:3000,https://app.example.com,https://admin.example.com
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Rate Limiting (Gateway level)
# These settings protect against brute force and DDoS attacks
# RATE_LIMIT_WINDOW_SECS: Time window in seconds (default: 60)
# RATE_LIMIT_MAX_REQUESTS: Maximum requests per window (default: 100)
# Adjust based on your traffic patterns
# RATE_LIMIT_WINDOW_SECS=60
# RATE_LIMIT_MAX_REQUESTS=100

# gRPC Services
IAM_AUTH_GRPC_ADDR=127.0.0.1:50051
